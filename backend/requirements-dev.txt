# requirements-dev.txt
# Development and testing dependencies — NOT installed in the production container.
# Install via: pip install -r requirements.txt -r requirements-dev.txt
# CI example:  pip install -r requirements-dev.txt && pytest

pytest==8.3.3
pytest-mock==3.14.0
httpx==0.28.1   # required by FastAPI's TestClient (starlette)
